import { PlaceholderPage } from "@/components/pages/PlaceholderPage";

export const metadata = { title: "talks — gsriram.dev" };

export default function TalksIndexPage() {
  return <PlaceholderPage tabId="talks/index.ts" />;
}
