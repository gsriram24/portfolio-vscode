import { PlaceholderPage } from "@/components/pages/PlaceholderPage";

export const metadata = { title: "stack.ts — gsriram.dev" };

export default function StackPage() {
  return <PlaceholderPage tabId="stack.ts" />;
}
